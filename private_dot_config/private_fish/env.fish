# paths
set --export PATH $HOME/.local/bin $HOME/.local/cargo/bin $HOME/.local/share/bin $PATH
set --export CARGO_HOME $HOME/.local/cargo
set --export LOCALE_ARCHIVE /usr/lib/locale/locale-archive # needed for nix to get locale
